# 6.Create a function that takes a string as input and returns the reversed string. For example, if the input is "Python", the output should be "nohtyP".

text = input("Enter the text:")
reversed = text[::-1]
print(reversed)